feat: integrate Clerk authentication with protected routes and auth pages

- Add @clerk/nextjs dependency and configure ClerkProvider in root layout
- Implement middleware with route protection for upload, cv, settings, and API routes
- Add public route matcher to allow unauthenticated access to auth pages
- Create sign-in and sign-up pages using Clerk components with catch-all routes
- Add main application layout with navbar, UserButton, and navigation
- Implement home page with authentication check and redirect logic
- Add authentication utility functions (getAuthUser, requireAuth, getUserId, isAuthenticated)
- Update app metadata with CV Engine branding
- Add environment variable template (.env.local.example)
- Add comprehensive documentation (ARCHITECTURE.md, CLERK_MIGRATION.md, MIGRATION_GUIDE.md, SETUP.md)
- Refactor route structure: move root page to (main) group, remove old auth pages

This commit establishes the authentication foundation for the CV Engine application,
enabling user sign-up, sign-in, and protected route access.

