fix: use admin client for storage uploads and improve UI

Fix uploadFile to use createAdminClient() instead of getSupabaseClient()
to ensure proper permissions for storage bucket uploads. Storage buckets
with RLS require the service role key for uploads.

Changes:
- Switch uploadFile to use createAdminClient() for storage operations
- Add security warning comment about server-side only usage
- Clarify that client-side uploads should use /api/cvs/upload endpoint
- Remove duplicate imports in CV detail API route
- Fix British English spelling: "Analyzing" → "Analysing"
- Add h-fit class to analysis loading state container for better layout

This ensures uploadFile has the correct permissions and prevents
potential permission errors when uploading files to private storage buckets.
