refactor: export types and use explicit imports

Export type definitions from types/index.d.ts and import them explicitly
instead of relying on global type declarations. This improves type safety
and makes dependencies clearer.

Changes:
- Export Job, Resume, and Feedback interfaces from types/index.d.ts
- Add explicit type imports in app/(main)/page.tsx
- Replace implicit global type usage with explicit imports

This ensures types are properly scoped and makes it easier to track
type dependencies across the codebase.
