refactor: improve Inngest function validation and retry strategy

Enhance the CV analysis Inngest function with better input validation and
optimized retry configuration to prevent unnecessary database writes while
ensuring reliable external API calls.

Changes:
- Add strict validation for imagePaths before processing
- Validate firstImagePath is a non-empty string before use
- Reduce function-level retries from 3 to 1 to avoid duplicate database writes
- Increase step-level retries for analyze-cv step from 2 to 3 (for OpenAI API)
- Add comments explaining retry strategy rationale
- Fix missing zod import in JOB_QUEUE_IMPLEMENTATION.md example code

The new retry strategy ensures:
- Database operations (writes) only happen once per function execution
- External API calls (OpenAI, storage) are retried at the step level
- Invalid inputs fail fast with clear error messages

This prevents race conditions from function-level retries while maintaining
reliability for external service calls.
