fix: correct error message extraction in CV upload analysis

Fix error message handling when OpenAI analysis fails. The error may be
a string rather than an Error object, so use it directly instead of
accessing the message property.

Changes:
- Use analysisError directly instead of analysisError?.message
- Move console.error after errorMessage is set for consistency
- Ensure error message is properly stored in database when analysis fails

This ensures that analysis errors are correctly captured and stored,
regardless of whether the error is a string or Error object.
