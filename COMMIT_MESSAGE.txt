feat: make feedback optional to support unanalyzed CVs

Make the feedback field optional in the Resume interface to support CVs
that haven't been analyzed yet. This allows the UI to display CVs while
analysis is in progress or if analysis fails.

Changes:
- Make feedback optional in Resume interface (feedback?: Feedback)
- Update CVCard to handle missing feedback with nullish coalescing (?? 0)
- Update CVDetailView to check for feedback existence before rendering
- Add explicit type imports from @/types in components
- Update test components page to conditionally render feedback-dependent components
- Update tests to use imported types and handle optional feedback
- Improve code formatting consistency in test files

This enables a better user experience by showing CVs immediately after
upload, even before OpenAI analysis completes.
