refactor: use atomic upsert in getOrCreateUser

Replace the read-then-insert pattern with a single atomic upsert operation
to prevent race conditions when multiple concurrent requests try to create
the same user record.

Changes:
- Use upsert with onConflict to atomically create or update user record
- Remove separate read and insert operations
- Simplify error handling with single upsert operation
- Add documentation explaining the atomic operation and race condition prevention

This ensures thread-safety and prevents duplicate key errors when multiple
requests attempt to create a user simultaneously, improving reliability
under concurrent load.
